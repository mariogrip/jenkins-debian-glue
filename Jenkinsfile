pipeline {
  agent any
  stages {
    stage('') {
      steps {
        sh '''# when using git:
/usr/bin/generate-git-snapshot

# when using subversion:
# /usr/bin/generate-svn-snapshot'''
      }
    }
  }
}